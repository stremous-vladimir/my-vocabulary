import DictionariesController from './dictionaries.controller.js'
import DictionariesTemplate from './dictionaries.template.html'

const DictionariesComponent = {
  bindings: {
  },
  templateUrl: DictionariesTemplate,
  controller: DictionariesController
};

export default DictionariesComponent;