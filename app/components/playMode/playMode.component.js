import PlayModeController from './playMode.controller.js'
import PlayModeTemplate from './playMode.template.html'

const PlayModeComponent = {
  bindings: {
  },
  templateUrl: PlayModeTemplate,
  controller: PlayModeController
};

export default PlayModeComponent;